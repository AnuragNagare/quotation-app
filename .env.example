# Neon Postgres connection string (from console.neon.tech → Connect)
# Used by the API at runtime (api/_lib/db.ts):
DATABASE_URL=
# Used by the migration scripts (scripts/*.mjs):
NEON_DATABASE_URL=
# Secret for signing session JWTs (any long random string):
JWT_SECRET=
